#include "main.h"

/**
 * _strlen - a function to calculate the
 * length of a string
 * @s: pointer to the string
 *
 * Return: the length of the string.
 */

int _strlen(char *s)
{
	int i;

	for (i = 1; s[i] != '\0'; i++)
	{
		;
	}

	return (i);

}

